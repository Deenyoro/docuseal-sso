diff --git a/Gemfile b/Gemfile
index 7ed8fdeb..61b895b4 100644
--- a/Gemfile
+++ b/Gemfile
@@ -26,6 +26,11 @@ gem 'jwt', require: false
 gem 'lograge'
 gem 'numo-narray-alt', require: false
 gem 'oj'
+gem 'omniauth-rails_csrf_protection'
+# omniauth-saml >= 2.2.1 requires ruby-saml ~> 1.18; ruby-saml >= 1.18.0 is the
+# fix for CVE-2025-25291/25292 (SAML auth bypass via parser differential).
+gem 'omniauth-saml', '>= 2.2.1'
+gem 'ruby-saml', '>= 1.18.0'
 gem 'onnxruntime', require: false
 gem 'pagy'
 gem 'pg', require: false
